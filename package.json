{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "api": "npm run dev --prefix api",
    "frontend": "npm run dev --prefix frontend",
    "test": "playwright test",
    "dev": "concurrently \"npm run api\" \"npm run frontend\"",
    "test:all": "start-server-and-test \"npm run dev\" http://localhost:5173 \"npm run test\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "concurrently": "^9.2.1",
    "start-server-and-test": "^2.1.5",
    "wait-on": "^9.0.10"
  }
}
